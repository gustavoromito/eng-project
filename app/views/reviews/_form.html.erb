<%= form_for([@event, @event.reviews.build]) do |f| %>
	
	<div id="rate-form">
		<label>Nota</label>
	</div>

	<div class="field">
		<%= f.label 'Comentário' %><br>
			<%= f.text_area :commentary, :id => "Comentário" %>
	</div>

	<div class="actions">
		<%= f.button 'Enviar', :value => :submit %>
	</div>
<% end %>

<%= render 'raty' %>
